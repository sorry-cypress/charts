apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sorry-cypress-helm.fullname" . }}-test-api-connection"
  labels:
    app.kubernetes.io/name: {{ include "sorry-cypress-helm.fullname" . }}
    helm.sh/chart: {{ include "sorry-cypress-helmchart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sorry-cypress-helm.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never